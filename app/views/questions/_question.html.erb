<div class="comment">
  <div class="comment-content">
    <%= render partial: 'stats', locals:{q:question ,votes:@question_votes} %>
    <%= link_to("#{question.title}", question_path(question))%>
    </div>
</div>